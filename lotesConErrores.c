#Falta el tipo 'int' o 'float'#
a = 5;   

#Falta el nombre de la variable#
int = 5; 
 
# Falta el tipo para el segundo parámetro#
int fsum(int a, b) {  
    return a + b;
}

#Falta el cierre de llave '}' y el punto y coma #

void print() {
    cout << "hola";  

    # Falta el paréntesis de apertura #
    if x > 5) {  
        cout << "Mayor";
    }

    # Condición del bucle incompleta #
    while (  
        cout << "un cartel";
}


int fcond() { 
    # Error en el uso del operador #
    int result = 5 + * 3;  
    # Falta la segunda parte de la comparación #
    if (a > ) {
        cout << "error";
    }   
}

int f2() {  
    #en el dimension del arreglo deberia ir constante entera, ademas falta , en inicializacion#
    int arr[aux] = {1, 2 3};
   # Falta el segundo operando para el operador lógico #
    if (a && ) {  
        cout << "Condicion valida";
    }
    # el operador para cin debería ser >>#
    cin << a; 
    cout << "Condicion valida";
}

# Falta el valor a retornar#
int fcalcu(int a, int b) {
    # Error: Falta el operador << #
    cout a << "hola siguien avanzando";
    cout << "Condicion valida";
    return;  
}

#falta llave que cierra#
void foo(int a, ) {
    #falta ;#
    int a = 5
    cout << "falta parametro";
    if (a == ) {
    cout << "falta comparacion";
} 
